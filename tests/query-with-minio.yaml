replicaCount: 3
config:
  query:
    tenantId: tenant1
    clusterId: cluster1
  storage:
    type: s3
    s3:
      endpoint_url: "http://minio.minio.svc:9000"
      access_key_id: "miniouser"
      secret_access_key: "miniopassword"
