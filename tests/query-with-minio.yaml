replicaCount: 3
config:
  query:
    tenantId: tenant1
    clusterId: cluster1
  storage:
    type: s3
    s3:
      endpoint: "http://minio.minio.svc:9000"
      accessKeyId: "miniouser"
      secretAccessKey: "miniopassword"
